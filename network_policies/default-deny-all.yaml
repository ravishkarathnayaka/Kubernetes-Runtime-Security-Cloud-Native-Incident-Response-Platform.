apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  labels:
    app.kubernetes.io/part-of: incident-response-platform
    security.policy: zero-trust-baseline
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
